apiVersion: apps/v1
kind: Deployment
metadata:
  name: microservice
  namespace: istio-experiments
spec:
  template:
    spec:
      containers:
        - name: application
          env:
            - name: OTHER_MICROSERVICE_ENDPOINT_TO_CALL
              value: microservice-c.istio-experiments.svc.cluster.local:8080/endpoint
