apiVersion: security.istio.io/v1
kind: AuthorizationPolicy
metadata:
  name: allow-nothing
spec:
  action: ALLOW
  # implementing the deny-by-default behavior for all workloads unless other ALLOW policies are
  # defined for workloads of interest, as no rules means nothing is ever matched
---
apiVersion: security.istio.io/v1
kind: AuthorizationPolicy
metadata:
  name: microservices-from-any-with-headers-to-a
  namespace: istio-experiments-sidecar-mode-application
spec:
  action: ALLOW
  rules:
  - to:
      - operation:
          methods: ["GET"]
          paths: ["/endpoint"]
          ports: ["8080"]
    when:
      - key: "request.headers[User-Agent]"
        values:
          - "a-very-handsome-client"
          - "another-very-handsome-client"
  selector:
    matchLabels:
      microservice: a
---
apiVersion: security.istio.io/v1
kind: AuthorizationPolicy
metadata:
  name: microservices-from-a-to-b
  namespace: istio-experiments-sidecar-mode-application
spec:
  action: ALLOW
  rules:
  - from:
      - source:
          namespaces: ["istio-experiments-sidecar-mode-application"]
          principals: ["cluster.local/ns/istio-experiments-sidecar-mode-application/sa/microservice-a"]
    to:
      - operation:
          methods: ["GET"]
          paths: ["/endpoint"]
          ports: ["8080"]
  selector:
    matchLabels:
      microservice: b
---
apiVersion: security.istio.io/v1
kind: AuthorizationPolicy
metadata:
  name: microservices-from-b-to-c
  namespace: istio-experiments-sidecar-mode-application
spec:
  action: ALLOW
  rules:
  - from:
      - source:
          namespaces: ["istio-experiments-sidecar-mode-application"]
          principals: ["cluster.local/ns/istio-experiments-sidecar-mode-application/sa/microservice-b"]
    to:
      - operation:
          methods: ["GET"]
          paths: ["/endpoint"]
          ports: ["8080"]
  selector:
    matchLabels:
      microservice: c
