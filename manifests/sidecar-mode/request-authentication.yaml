apiVersion: security.istio.io/v1
kind: RequestAuthentication
metadata:
  name: no-jwt-validation-for-microservice-a
  namespace: istio-experiments-sidecar-mode-application
spec:
  selector:
    matchLabels:
      microservice: a
  jwtRules: []
