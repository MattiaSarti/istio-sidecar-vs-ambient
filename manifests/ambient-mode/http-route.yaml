apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: microservices
  namespace: istio-experiments-ambient-mode-application
spec:
  parentRefs:
    - name: microservices-ingress-gateway
  hostnames:
    - completely.made.up.host.com
  rules:
    - backendRefs:
        - name: microservice-a
          port: 80
      filters:
        - type: URLRewrite
          urlRewrite:
            path:
              type: ReplaceFullPath
              replaceFullPath: /endpoint
      matches:
        - path:
            type: Exact
            value: /a
      retry:
        attempts: 30
        backoff: 500ms
        codes: [500]
      timeouts:
        BackendRequest: 30s
        request: 35s
    - backendRefs:
        - name: microservice-b
          port: 80
      filters:
        - type: URLRewrite
          urlRewrite:
            path:
              type: ReplaceFullPath
              replaceFullPath: /endpoint
      matches:
        - path:
            type: Exact
            value: /b
      retry:
        attempts: 30
        backoff: 500ms
        codes: [500]
      timeouts:
        BackendRequest: 30s
        request: 35s
    - backendRefs:
        - name: microservice-c
          port: 80
      filters:
        - type: URLRewrite
          urlRewrite:
            path:
              type: ReplaceFullPath
              replaceFullPath: /endpoint
      matches:
        - path:
            type: Exact
            value: /c
      retry:
        attempts: 30
        backoff: 500ms
        codes: [500]
      timeouts:
        BackendRequest: 30s
        request: 35s
